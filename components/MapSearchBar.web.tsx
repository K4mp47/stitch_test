import React from 'react';
import { View, Text } from 'react-native';

const MapSearchBar = () => {
  return (
    <View>
      <Text>Map search bar is not available on the web.</Text>
    </View>
  );
};

export default MapSearchBar;
