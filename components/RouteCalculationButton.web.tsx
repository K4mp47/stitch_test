import React from 'react';
import { View, Text } from 'react-native';

const RouteCalculationButton = () => {
  return (
    <View>
      <Text>Route calculation button is not available on the web.</Text>
    </View>
  );
};

export default RouteCalculationButton;
